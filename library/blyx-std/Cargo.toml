[package]
name = "blyx-std"
version = "0.1.0"
edition = "2024"
description = "Blyx Standard Library — AI-native runtime, I/O, networking, and agent coordination"

[dependencies]

[features]
default = []
llm = []
agents = []
