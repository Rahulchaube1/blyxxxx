[package]
name = "blyx_bir"
version = "0.1.0"
edition = "2024"
description = "Blyx Intermediate Representation (BIR) — SSA-form IR, optimizer passes, and LLVM IR emitter"

[dependencies]
blyx_ast = { path = "../blyx_ast" }
