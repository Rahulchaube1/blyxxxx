name: Container Registry

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    name: Container workflow placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: echo "Blyx container publishing will be enabled when an official container image is defined."
